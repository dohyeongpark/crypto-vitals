services:
  collector:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: "no"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
